<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meetball.demo.persistence.OrderMapper">
    <insert id="insertOrder" parameterType="com.meetball.demo.domain.Order">
          INSERT INTO orders (orderId, ownerName, driverName, carNumber, beginStr, endStr, amount)
              VALUES   (#{orderId}, #{ownerName}, #{driverName}, #{carNumber}, #{beginStr}, #{endStr}, #{amount})
    </insert>

    <select id="getOrderList" resultType="String">
        SELECT orderId FROM orders WHERE (ownerName = #{userName} OR driverName = #{userName})
    </select>

    <select id="getOrderInfo" resultType="com.meetball.demo.domain.Order">
        SELECT * FROM orders WHERE orderId = #{orderId}
    </select>


</mapper>